---
title: "Traspaso: tokmd — PBI-006 cerrado, PBI-007 con código listo (QA en Kiwi a mitad de camino), corta por contexto"
aliases:
  - "Handoff tokmd 20260925-1016"
project: tokmd
document_type: work-plan
status: active
version: 0.1.0
created: 2026-09-25
updated: 2026-09-25
language: es
owners:
  - project-founder
author_human: "none"
created_by: "llm"
llm_provider: "Anthropic"
llm_model: "claude-sonnet-5"
llm_harness: "Claude Code Desktop"
llm_channel: "subscription"
reasoning_mode: "no expuesto"
last_modified_by: "Anthropic / claude-sonnet-5 / Claude Code Desktop / subscription"
modified_by: "Anthropic / claude-sonnet-5 / Claude Code Desktop / subscription"
reviewed_by: "pending"
review_status: "pending"
source_of_truth: true
tags:
  - project/tokmd
  - handoff
related_documents:
  - "[[HANDOFF-fase-2-pbi002-003-004-20260925]]"
---

# Traspaso: tokmd — PBI-006 cerrado, PBI-007 con código listo (QA en Kiwi a mitad de camino), corta por contexto

## Historial de modificaciones

| Fecha | Versión | Modificado por | Descripción |
|---|---|---|---|
| 2026-09-25 | 0.1.0 | Anthropic / claude-sonnet-5 / Claude Code Desktop / subscription | Creación. Primera prueba de traspaso entre sesiones vía `claude --bg --name`, a pedido explícito de Fabián — corta por límite de contexto/compactación (no por el tope de 300 llamadas, que ya se había pasado con autorización de Fabián horas antes). |

## En una línea

PBI-006 cerrado de punta a punta. PBI-007 tiene el código listo, pusheado y verde (CI real en GitHub Actions, Sonar OK) — **pero su QA en Kiwi quedó a mitad de camino**: los 4 casos están cargados como PROPOSED, sin confirmar, sin Test Run. Primer paso concreto de esta sesión: terminar eso. PBI-008 sigue bloqueado por dos decisiones de Fabián sin cerrar.

## Repositorio y commit actual

`C:\IA\Projects\Claude-Tokenizer`, rama `main`, remoto `https://github.com/fferdgelis/tokmd` (público, pusheado). Último commit: `e13f8d1` (cierre de QA de PBI-006). Verificar con `git log --oneline -10`.

## Qué existe y dónde

- **Traspasos anteriores:** `docs/handoff/HANDOFF-fase-0-y-1-completadas-20260925.md` y
  `docs/handoff/HANDOFF-fase-2-pbi002-003-004-20260925.md` — siguen vigentes en todo lo
  que no se contradiga acá.
- **PBI-001 a 006:** cerrados de punta a punta (dev, TDD, QA independiente en Kiwi,
  Sonar). PBI-006 se cerró en esta sesión.
- **PBI-007:** código completo, hecho por **Antigravity** (delegado explícitamente por
  Fabián para no gastar ventana de Claude), no por esta sesión directamente — ver
  sección propia abajo. Commit `9fc712d`.
- **PBI-008:** sin empezar, bloqueado (ver sección propia abajo).

## PRIMER PASO CONCRETO: terminar el QA de PBI-007 en Kiwi

Esto es lo que quedó a mitad de camino cuando esta sesión se cortó — arrancar por acá:

1. Los 4 casos de PBI-007 ya están cargados en Kiwi como **PROPOSED**, sin confirmar:
   - id 366 — `TOK-007-C01 - CI verde en ubuntu y windows, Python 3.12 y 3.13`
   - id 367 — `TOK-007-C02 - publish.yml usa Trusted Publishing sin token en el repo`
   - id 368 — `TOK-007-C03 - tokmd --version responde 1.0.0`
   - id 369 — `TOK-007-C04 - README con ejemplo real y creditos`
2. Fabián ya autorizó explícitamente esta sesión (y la anterior) a confirmar casos
   PROPOSED por API cuando hace falta (`tools/kiwi/confirmar_casos.py`, ya construido y
   probado) — no hace falta volver a pedirle permiso para ESTE mecanismo puntual, salvo
   que algo se vea raro.
3. Crear el Test Run (`tools/kiwi/crear_run.py`) contra el plan
   `PBI-007 - Empaquetado y publicacion`, commit `9fc712d`, con `manager` y `qa` reales
   (`claude-code-sonnet` / `opencode-kimi-k3` — son los defaults del script, no hace
   falta pasarlos a mano).
4. Escribir `tools/qa/brief-qa-pbi007.md.prompt` mapeando los 4 casos a evidencia real:
   - AC-01 (CI): **no es un test de pytest** — verificar con
     `gh run list --limit 5` / `gh run view <id> --json conclusion,jobs` que el run más
     reciente de `CI` en `main` tiene `conclusion: success` en los 4 jobs
     (ubuntu/windows × 3.12/3.13). Ya se verificó una vez en esta sesión con el run
     `36136759090` (4/4 success) — repetir contra el run más reciente al momento de
     correr QA, no asumir que sigue siendo el mismo.
   - AC-02 (publish.yml): leer `.github/workflows/publish.yml` y confirmar que usa
     Trusted Publishing (OIDC, `permissions: id-token: write`, sin ningún secreto de
     PyPI en el repo) y que el `environment` coincide exactamente con lo registrado en
     PyPI (`pypi`).
   - AC-03 (`--version`): pytest test `test_version_flag_succeeds` en `test_cli.py`
     (ya existe, ya pasa).
   - AC-04 (README): leer `README.md` y `README.es.md`, confirmar que el ejemplo de
     salida es real (no inventado) y que hay créditos a `ctok` y `ttok`.
5. Correr `tools/qa/Invoke-QA-Kimi.ps1` sobre un snapshot (`git archive 9fc712d`, o el
   commit que sea HEAD en ese momento) con ese brief.
6. Aplicar el veredicto con `registrar_resultados.py` (`tested_by: opencode-kimi-k3`).
7. Cerrar el PBI-007 en su doc (`docs/PBI/PBI-007-empaquetado-y-publicacion.md`,
   sección 6) y en el dev-log.

**Ojo con un problema real ya encontrado hoy:** la primera vuelta de QA de PBI-006 dio
un FAILED falso porque el brief le pedía a QA correr `git status` DENTRO de un snapshot
de `git archive` (que no tiene `.git`, no es comparable). Para AC-01 de PBI-007 hay el
mismo riesgo al revés: NO se puede verificar el CI real desde dentro del snapshot
tampoco — el snapshot no tiene acceso a GitHub. Hay que decidir si QA (Kimi K3, que
corre con `--dir` sobre el snapshot, sin red real más que lo que ya tiene OpenCode) puede
correr `gh run view` desde ahí, o si esa verificación puntual (AC-01) la hace
**esta sesión directamente antes de armar el brief** y se le pasa a QA como dato ya
verificado en el texto del caso, en vez de pedirle que lo repita. Recomendación: la
segunda opción — ya se verificó una vez manualmente en esta sesión (run `36136759090`),
es más simple que darle acceso de red real a QA.

## PBI-007: qué hizo Antigravity, verificado (no confiar sólo en su propio resumen)

Delegado explícitamente por Fabián vía `mcp__panel__ask_antigravity` (dos subagentes
en background: uno para el desarrollo, otro para una prueba de canario real de Sonar
aparte, ver abajo). Verificado por esta sesión, no sólo tomado del dicho de Antigravity:

- `.github/workflows/ci.yml` (matrix ubuntu/windows × 3.12/3.13) — **CI real corrido y
  verde**, confirmado con `gh run view 36136759090`: 4/4 jobs `success`.
- `.github/workflows/publish.yml` — Trusted Publishing, `environment: pypi`, nombre de
  archivo exacto — coincide con el pending publisher ya registrado en PyPI. **No
  verificado a fondo el contenido línea por línea en esta sesión**, sólo que existe y
  que el nombre/environment coinciden — revisarlo con calma si hay dudas.
- `README.md`, `README.es.md`, `CHANGELOG.md` — creados.
- `src/tokmd/cli.py`: agregó `--version` (`click.version_option` +
  `importlib.metadata.version("tokmd")`).
- `tests/test_cli.py`: test nuevo `test_version_flag_succeeds`.
- **38/38 tests verdes**, confirmado corriendo `uv run pytest -q` directamente (no sólo
  el dicho de Antigravity).
- **Sonar Quality Gate: OK**, confirmado por esta sesión corriendo
  `tools\sonarqube\Invoke-SonarGate.ps1` directamente (no sólo el dicho de Antigravity).
- **Tag `v1.0.0`: NO existe** (confirmado con `git tag`). No se disparó ninguna
  publicación real a PyPI. Sigue así a propósito — publicar es irreversible y Fabián
  quiere confirmarlo aparte, en otro momento, no como parte de este flujo automático.

## Canario real de Sonar con código malo (hecho, con un hallazgo abierto)

A pedido de Fabián: se le pidió a Antigravity escribir código real y deliberadamente
malo (secreto hardcodeado, función con complejidad cognitiva altísima, código
duplicado) en una **copia aislada** del repo (nunca el repo real — SonarQube Community
no soporta ramas, así que se usó una clave de proyecto descartable,
`tokmd-canario-real-f628ea42`, para no ensuciar el historial de `tokmd`). Resultado:

- **Gate: ERROR** — `new_violations=2`, `new_duplicated_lines_density=45.0%` (umbral 3%),
  `new_coverage=6.2%` (umbral 80%).
- Cazó: complejidad cognitiva (99 contra el límite de 15, `python:S3776`) y un code
  smell de duplicación de literal (`python:S1192`).
- **NO cazó el secreto hardcodeado** (`ANTHROPIC_API_KEY = "sk-ant-api03-..."`) — ni
  issue ni security hotspot, verificado consultando `/api/hotspots/search`. **Hallazgo
  abierto, sin investigar**: por qué las reglas de detección de secretos de SonarQube no
  dispararon con ese patrón. Vale la pena mirarlo en algún momento, no bloquea nada.
- Evidencia completa: `C:\IA\Data\sonarqube\reports\tokmd-canario-real\20260925-100525\`.
- El repo real quedó intacto, verificado con `git status` después de la limpieza.

## PBI-008: bloqueado, dos decisiones de Fabián sin cerrar

1. **¿Cablear `--verify` en `cli.py`?** Fabián estuvo de acuerdo en que tiene sentido
   (es literalmente lo que pide la historia de usuario de PBI-008: el número exacto,
   no una aproximación), pero la pregunta que quedó sin responder es si tiene una
   `ANTHROPIC_API_KEY` real con facturación habilitada a mano — sin eso, cablear el
   flag no sirve para nada esta noche. **Preguntarle directo antes de tocar nada de
   PBI-008.**
2. PBI-008 depende de PBI-007 publicado (o al menos utilizable) — con PBI-007 todavía
   sin el tag/publicación real, formalmente sigue bloqueado por dependencia también.

## Trazabilidad de Kiwi: cerrado, con hallazgo forense importante

- Los 24 casos de tokmd tienen `is_automated=true` (y de hecho **149 en toda la
  instancia de Kiwi**, otros productos incluidos — autorizado explícitamente por
  Fabián como owner de todo, con el aviso de seguridad del propio arnés
  "Modify Shared Resources" mostrado y aceptado).
- `author`/`manager`/`tested_by` ahora usan identidades reales
  (`claude-code-sonnet`, `opencode-kimi-k3`) en vez de `qa-bot` genérico — cableado en
  `cargar_casos.py`, `crear_run.py`, `registrar_resultados.py`, y aplicado
  retroactivamente a TR-61 a TR-65.
- **`User.create` no existe en la API de Kiwi** — cuentas nuevas por combinación
  motor+harness se dan de alta a mano en `/admin`, nunca por script.
- **Hallazgo forense, verificado contra Postgres directo** (`kiwi_prod_reader`, solo
  lectura): las 5 cuentas de identidad (`codex-orchestrator`, `claude-code-sonnet`,
  `opencode-qwen3-coder-next`, `opencode-kimi-k3`, `claude-code-opus-5`) **no se
  crearon desde el panel de administración** — `django_admin_log` no tiene ninguna
  fila para ellas, y 4 de las 5 se crearon en una ventana de 18 milisegundos entre sí
  (imposible a mano). Alguna sesión anterior las creó por fuera de la UI (probablemente
  `manage.py shell`), sin dejar rastro de cuál.

## Cómo seguir (pedido explícito de Fabián para esta sesión y las que vengan)

Avanzar sola, PBI por PBI: programar (o delegar a Antigravity si Fabián lo pide
explícitamente, como con PBI-007), TDD real, QA independiente en Kiwi (Kimi K3 vía
OpenCode — nunca un veredicto propio), Sonar en OK, y recién ahí seguir con el
siguiente. Pausar a preguntar sólo cuando haga falta una decisión real de Fabián o una
acción que sólo él pueda hacer (tag/publicación real, credenciales, plata) — no por
costumbre.

**Separación de roles, no negociable** (motivo: Fabián frenó esta sesión en seco una
vez por reportar mal esta distinción): Desarrollo escribe y corre pytest para
implementar — eso está bien, es necesario, no se reporta como "QA passed". El
veredicto que se registra en Kiwi sale exclusivamente de QA independiente (Kimi K3 vía
OpenCode sobre un snapshot de solo lectura), nunca de una corrida propia de Desarrollo.
