You are the independent QA role (ADR-005/ADR-006). You did NOT write the code
and did NOT write the tests. You are working read-only against a snapshot of
a git commit — do not edit any file, do not write any new file anywhere.

## Your job

For each Kiwi test case below, actually run the relevant pytest test(s) in
this checkout and report a verdict. Do not guess or reason about whether the
code "should" work — execute `uv run pytest -k <name> -v` (or the full
suite) and read the real output. If a command fails to run at all, that is
BLOCKED, not FAILED.

Do not fix anything. Do not edit or create any file. If you find a real
defect, report it as FAILED with the raw pytest output as evidence — do not
patch it.

## Test cases (PBI-006 — Verificación contra la API)

- TOK-006-C01 - Sin ANTHROPIC_API_KEY, error legible
  -> pytest test: test_get_client_raises_missing_api_key_error
- TOK-006-C02 - count_verified resta el marco medido
  -> pytest tests: test_measure_frame_returns_minimal_content_tokens,
     test_count_verified_returns_real_tokens_minus_frame
- TOK-006-C03 - Ninguna llamada de red real en la suite de tests
  -> pytest test: test_no_real_anthropic_import_in_this_module
- TOK-006-C04 - Mutante del parser de front matter, cazado por el test suite
  -> NOT a pytest test you run yourself (the mutation was already performed
     and reverted in an earlier session, on 2026-09-25 — do NOT repeat the
     mutation, this checkout has no .git directory to compare against
     anyway, it's a plain export). Verify two things instead:
     1. In the full suite run above,
        `test_front_matter_becomes_root_child_with_content` PASSED — that
        alone proves `src/tokmd/sections.py`'s front-matter parsing is NOT
        currently broken (if the earlier mutation had not been fully
        reverted, this exact test would fail).
     2. Open and read docs/PBI/PBI-006-verificacion-contra-api.md and
        confirm it documents the mutation exercise: FRONT_MATTER_RE broken
        on purpose, exactly one test failed pointing at the break, then
        reverted.
     If both hold, PASSED.

## Output format

Run `uv sync` then `uv run pytest -v` once for the whole suite, and report
the raw output. Then, for each of the 4 cases above, output one line in
exactly this format (no extra prose):

VEREDICTO: <exact case name from the list above> | <PASSED|FAILED|BLOCKED> | <one-line evidence>

Print the raw pytest output first, then the four VEREDICTO lines, then
nothing else. Do not write your report to any file — this response IS the
report.
